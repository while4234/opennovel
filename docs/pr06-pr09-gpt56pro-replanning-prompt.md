# GPT-5.6 Pro：PR-06～PR-09 完整重规划提示词

把本文件全文交给 GPT-5.6 Pro。它只负责检查真实仓库并重新输出开发文档，不直接开发。

```text
你正在为一个真实的长篇小说创作系统重新规划 PR-06～PR-09。请以“专业小说创作质量优先、普通用户易用、长上下文必须分批”为最高原则，先检查真实仓库，再输出一份可以直接交给后续开发代理执行的完整 PR 开发文档。

仓库与基线：
- 仓库：D:\ainovel
- GitHub：https://github.com/while4234/ainovel-cli.git
- 工作分支：main
- PR-01～PR-05 已合并。PR-05 正在关闭 pending structure migration 与受保护正式写入重复进入非重入 revision transaction 的最后问题。
- 先完整读取 `.codex/HANDOFF.md`、`GIT_NOTES.md`、`docs/pr05-known-pending-migration-deadlock.md`、`AGENTS.md`（若存在），再检查 PR-01～PR-05 的真实代码、接口、状态机、测试和 Web UI。
- 以真实代码为准映射名称，不重新规划 PR-01～PR-05。把 PR-05 最终测试通过列为 PR-06 的硬前置条件。
- 小说续写不新增同类功能，但必须回归保护。

模式边界：
1. 普通共创没有原著、source anchors 或原著事件，只能依据原创设定、人物、世界规则、前文正文和故事状态；禁止读取、伪造或泄漏改编 source 字段。
2. 小说改编拥有不可变的原著清单、文本证据、事件账本和 source anchors。新增目标章必须重新分配原著覆盖或明确 IsAdded；不得遗漏原著、改变受保护主线或绕过 preserve events、required changes、forbidden moves、事件所有权、关系/设定声明和粒度字数合同。目标章节稳定 ID、显示章号和原著章节号必须严格区分。
3. 两种模式只共享修订流程基础设施，不共享规划提示词、审核合同和语义标准。

产品目标：
- 分卷、弧、章节提纲和正文都是可持续修订、扩写、恢复、追溯的活稿件，不再首次提交后锁死。
- 提案完成后、提纲生成中、正文写作中和完本后均可反向修改结构、提纲或正文。
- 初始章节数和总字数是动态软预算；支持扩写当前章、任意位置插章、追加章/弧/卷/尾声、拆分未完成章和移动未写章节归卷。本轮不支持删除或合并已写成章节。
- 显示章号可重排，稳定 ID 与旧正文身份不变；原著章节号永不重排。
- 单章提纲只变化一章时默认只重写该章。额外返工必须有因果、人物状态、伏笔、时间线或原著覆盖证据，并让用户确认；原生成批次不是整批重写理由。
- 写作中可以先打磨已完成章节再继续。正文意见若改变剧情合同，必须升级为提纲/结构修订预览。
- 旧稿在候选审核和人工批准前不能覆盖；自动审核失败必须停留，自动/定时恢复不能越过修订或人工确认。

固定流程：
安全暂停 → 影响预览 → 用户确认 → 分卷/弧候选及卷级、相邻承接、分批全书、全书骨架复审 → 人工确认 → 受影响章节提纲候选及单章、弧、卷、分批全书、全书细纲复审 → 人工确认 → 新写/重写/打磨正文候选及文学、连续性、模式专属审校 → 人工确认 → 重建摘要、事实、人物状态、伏笔、关系和后处理 → 恢复创作。

UI 决策：
- 正文阅读和修改主入口在“状态”页，升级为稿件工作区；“共创”只负责讨论，可携带受限的当前章/提纲/分卷上下文。
- 左侧卷弧章树；中央正文、章节提纲、所属分卷、审核、历史标签；支持当前稿/候选稿对照、旧版本查看和通过新修订会话恢复。
- 写作中可阅读全文；保留实时创作；支持长文性能、桌面/移动、键盘和 ARIA。
- 默认扩写表单只有位置、一句话和“让 AI 规划”。AI 判断扩写原章、插章、多章、小弧、新卷或完本追加，并展示位置、章数、字数区间、结构摘要、required/recommended 影响、证据、审核链和模式约束。
- 提供“更紧凑”“更充分”“单独成卷”“修改描述”；高级设置折叠；禁止默认暴露内部 ID/签名/事件账本，禁止复杂拖拽图编辑器。

AI 与批处理约束：
- 不能一次生成过多内容或携带整本书/整部原著。按卷、弧和约 3～4 章语义批次生成，但生成批次不得成为正文返工范围。
- 每次模型输入必须有 token/字符预算、证据窗口和摘要窗口。超预算时先形成结构化摘要、事实快照和依赖清单，再分批处理。
- 改编只加载当前任务所需原著片段、相邻证据和合同；普通共创只加载相关设定、前文摘要、人物状态、目标章及必要相邻正文。
- 全书审核采用局部 → 卷级聚合 → 分批全书 → 全书签名/骨架总审。所有结果绑定内容签名，上游变化后旧 pass 失效。
- 设计截断、空响应、格式错误、失败批次的持久化恢复，并为每个 PR 明确上下文裁剪、批次上限、失败恢复和质量测试。

重新规划范围：
- PR-06：写作期与完本后的正文打磨、重写和最小返工队列。
- PR-07：状态页稿件工作区。
- PR-08：一句话补剧情、插章与扩写交互。
- PR-09：恢复、兼容、导出、性能、文档、真实项目克隆和全链路验收。
可调整四个 PR 内部职责与名称，但只能输出 PR-06～PR-09；每个 PR 必须独立可审查、修复、复审、提交；PR-09 不能承接此前遗漏的核心功能。

PR-06 必须覆盖：
- writing/complete 正文可读可修订；打磨保持剧情合同和章末状态，重写受批准提纲约束；剧情变化自动升级。
- 版本化候选隔离，批准后才替换并重建真实影响范围的摘要、事实、状态、伏笔、关系和后处理。
- 普通共创做文学与连续性审核；改编重新读取原著证据并执行合同、AdaptationCheck 和独立语义审计。
- 稳定章节 ID 精确返工；单章变化只重写单章；明确批处理、上下文裁剪、失败恢复、旧稿安全和恢复顺序。

PR-07 必须覆盖：
- 状态页卷弧章树、完整稿件工作区、写作中正文、正文/提纲/分卷/审核/历史、当前/候选对照、旧版本与恢复入口。
- 共创受限上下文入口，长文虚拟化或等效性能，桌面/移动/键盘/ARIA，保留实时流。
- 前端状态、API 缓存失效、错误恢复、Vitest 和浏览器测试。

PR-08 必须覆盖：
- 顶部“补剧情/扩写”、章节间“＋补充剧情”、完本“继续扩写”。
- 一句话自适应建议、清晰预览、快捷调整、人工确认、完整步骤条、动态软预算。
- 双模式文案与覆盖提示；改编目标章/原著章不混淆；预览过期、并发修订、取消、重试和错误恢复；真实桌面/移动浏览器验收。

PR-09 必须覆盖：
- 活动修订崩溃恢复、定时/自动恢复阻断、预览过期、expected_revision、幂等、取消重试、版本恢复、完本扩写重新开启和重新完成。
- 稳定 ID、数字兼容 API、按显示顺序导出且原著号不变、摘要/事实快照性能、旧项目迁移克隆。
- 普通共创、改编、完本返工、初始审核和小说续写回归；README、架构和用户文档。
- 一个真实普通共创项目克隆和一个真实改编项目克隆的端到端验收。只读发现后复制，严禁修改或提交真实项目；说明匿名化、克隆目录、清理、证据留存和失败隔离。
- 克隆场景至少包括：写作中读正文、中途插章、新弧、新卷、单章提纲只重写单章、正文打磨、分卷修改后卷级和全书复审、完本追加、候选失败/拒绝不丢稿、重启续修、普通模式无 source、改编覆盖/合同/正文重审、自动恢复人工门禁、一句话完整流程、模型上下文确实分批且未越预算。

每个 PR 必须写清：
- 目标/非目标、用户场景、真实代码落点、数据模型、API、状态转换、模式边界、AI 提示/上下文/批次、UI、迁移兼容、并发幂等恢复、安全隔离、测试、浏览器验收、机械门禁、审查重点、完成定义和下一 PR 边界。
- 给出可直接复制的 Codex develop prompt、after development review prompt、recursive fix prompt template、post-fix re-review prompt。
- 审查 decision 只能为 pass|needs_fix|blocked|invalid；非 pass 列 Blocker/Major/Minor 并生成精确修复和复审提示；小修也必须修复后复审。

工程约束：
- 不允许 TODO、伪实现、测试后门、放宽质量门禁、无关重写、破坏数字 API/续写/初始审核/实时创作。
- 每个 PR 独立 commit；未获授权不默认推送。Windows 上 Go/UI 门禁串行。
- 建议门禁：`git diff --check`、changed-file gofmt、`go test -p 1 ./... -count=1`、`go vet -p 1 ./...`、`npm.cmd --prefix internal/entry/web/ui test -- --run`、UI build、静态产物和 bootstrap/assets 隔离。故障注入/幂等/跨 Store/跨进程测试 `-count=5`。race 不可用时如实记录并用确定性超时和跨实例测试补偿。

专业作家质量标准：
- 判断修改是否改变人物欲望、阻力、选择、代价、结果、章末状态和后续承诺；新增剧情是否构成独立戏剧单元；是否破坏卷级节奏、高潮和不可逆出口；是否造成伏笔悬空、状态倒退、因果断裂或信息重复。
- 据此决定扩写原章、插章、小弧或新卷，以及是否存在可证明的下游返工；改编必须保持原著覆盖与合同；审核必须绑定当前签名；模型上下文不能过大。

最终输出一份完整 Markdown 文档，至少包含：
1. 真实仓库现状核对；2. PR-05 前置门槛；3. PR-06～PR-09 划分和依赖图；4. 跨 PR 不变量；5. 双模式边界；6. 数据模型/API 总览；7. AI 分批策略；8. 各 PR 完整规格；9. 四类可复制提示词；10. 测试矩阵；11. 真实项目克隆验收；12. 机械门禁；13. 风险回滚兼容；14. 最终验收；15. 假设和非目标。

文档必须引用真实文件、包、接口和测试位置；建议新增文件时给出路径与职责。不要修改代码，不要启动子代理或 pr-pipeline，不要创建分支、提交或推送。真实代码与旧假设冲突时，以真实代码为准并明确差异。先检查仓库，再输出文档。
```
