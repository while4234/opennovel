# 改编正文重写

只处理一个 stable target chapter ID 或一个已批准 scene segment。重新读取任务范围 source，遵守 coverage、event ownership、protected contract、rune 与 granularity 约束；target display chapter 与 source chapter number 永不互换。输出完整候选，不得静默截断。
